{% comment %}
  This section is used in the product template to render product page with
  media, content, and add-to-cart form.

  https://shopify.dev/docs/storefronts/themes/architecture/templates/product
{% endcomment %}

<div class="w-64">
  {% for image in product.images %}
    {% if forloop.first %}
      <div style="view-transition-name: photo-{{product.title | handleize }}">
        {% render 'image', class: 'product-image w-36', image: image %}    
      </div>
    {% else %}
      {% render 'image', class: 'product-image w-36', image: image %}
    {% endif %}
  {% endfor %}
</div>

<div class="product-info">
  <p class="font-mono" style="view-transition-name: title-{{product.title | handleize }}">{{ product.title }}</p>
  <p class="">{{ product.price | money }}</p>
  <p>{{ product.description }}</p>
</div>

<div class="product-form">
  {% form 'product', product %}
    {% assign current_variant = product.selected_or_first_available_variant %}

    <select name="id">
      {% for variant in product.variants %}
        <option
          value="{{ variant.id }}"
          {% if variant == current_variant %}
            selected="selected"
          {% endif %}
        >
          {{ variant.title }} - {{ variant.price | money }}
        </option>
      {% endfor %}
    </select>

    <input type="text" name="quantity" min="1" value="1">

    <input type="submit" value="Add to cart">
    {{ form | payment_button }}
  {% endform %}
</div>

{% schema %}
{
  "name": "t:general.product",
  "settings": [],
  "disabled_on": {
    "groups": ["header", "footer"]
  }
}
{% endschema %}
